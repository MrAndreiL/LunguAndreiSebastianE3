#pragma once

// Functions to compare names.
static int stringLength(const char* str);

int compareName(const char* name1, const char* name2);

// Functions to compare grades.
int compareGrade(const float grade1, const float grade2);
